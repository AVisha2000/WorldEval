# Conformance fixtures

Each `valid.*.json` file is accepted by the correspondingly named schema. The
decision-response directory contains one fixture for every member of the strict
tagged union. Runtime tests additionally materialize and validate both complete
Primitive Sandbox episode initializations and replay traces. The replay fixture
proves that authority-derived safety counters are present and schema-bound.
