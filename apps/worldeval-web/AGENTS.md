# WorldEval web agent rules

- This package contains both the Controller Lab and static WorldEval site entrypoints.
- Browser projections must remain allow-listed and must never expose protected replay files,
  provider output, prompts, credentials, or authority-only observations.
- Preserve both local and Pages builds and run lint, tests, typecheck, and both builds after
  changes.
