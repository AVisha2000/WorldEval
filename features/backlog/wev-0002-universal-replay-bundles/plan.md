# Implementation plan

Define and validate the outer bundle, implement safe staging and offline replay
verification, expose public allow-listed artifacts, then normalize accepted demos
without rewriting frozen inner replays.
