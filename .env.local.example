# Local-only credentials for testing live Controller Lab games.
#
# Copy to .env.local, set a newly rotated provider key, then keep the file mode 0600.
# The copied .env.local is gitignored; production deployment does not read it.
WORLDARENA_OPENAI_API_KEY=
